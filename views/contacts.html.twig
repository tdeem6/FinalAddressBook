<html>
<head>
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css'>
    <link rel="stylesheet" href="css/styles.css">
    <title>Contacts of Address Book</title>
</head>
<body>
    <h1>Contacts stored:</h1>
    {% if allcontacts is not empty %}
        <p>Here are the contacts you've entered:</p>
        <ul>
            {% for contact in contacts %}
                <li>Name: {{ contact.getName }}</li>
                <li>Phone number: {{ contact.getPhone }}</li>
                <li>Address: {{ contact.getAddress }}</li>
            {% endfor %}
        </ul>

    {% endif %}




<h1>Enter in your contact.</h1>
        <form action='/contacts' method='post'>
            <div class='form-group'>
                <label for='name'>Contact name:</label>
                <input id='name' name='name' class='form-control' type='text'>
            </div>
            <div class='form-group'>
                <label for='phone'>Contact phone number:</label>
                <input id='phone' name='phone' class='form-control' type='number'>
            </div>
            <div class='form-group'>
                <label for='address'>Contact address:</label>
                <input id='address' name='address' class='form-control' type='text'>
            </div>
            <button type='submit' class='btn-success'>Hit it!</button>
        </form>
      </br>
      </br>
      </br>
      <form action='/contacts_deleted' method='post'>
          <button type='submit' class='btn-danger'>Delete your contacts!</button>
      </form>
  </div>
